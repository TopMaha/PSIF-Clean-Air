# PSIF Clean Air — ระบบบันทึก & ติดตามความปลอดภัย

**เวอร์ชันปัจจุบัน: v2.8** (แสดงที่หน้า login และแถบบนสุดของแอป · ฝั่ง API ดูได้ที่ `GET /health`)

เว็บแอป (ไฟล์เดียว) สำหรับบันทึก PSIF (Plant Safety Improvement Form) พร้อมระบบติดตาม 4 ขั้นตอน,
รูปภาพบังคับ, Safety ตรวจสอบ, ตรวจเรื่องซ้ำ, รายงานรายคน/ภาพรวม, แดชบอร์ด, ล็อกอินด้วยรหัสพนักงาน
และเป้าหมายแยกรายประเภท — Cloudflare Worker + D1 + R2

> 🏭 **ชุดของ TENNECO Clean Air** — โค้ดชุดเดียวกับ PSIF ของโรงงานเดิม แต่ **แยกทุกอย่างขาดจากกัน**:
> คนละ Worker · คนละฐานข้อมูล D1 · คนละถัง R2 · คนละที่เก็บค่าในเบราว์เซอร์
> ข้อมูลสองระบบไม่ปนกัน และแก้อีกฝั่งไม่กระทบกัน
>
> ⚠️ **กติกาที่ต่างจากโรงงานเดิม (v2.8)**
> | เรื่อง | PSIF โรงงานเดิม | **TENNECO Clean Air** |
> |---|---|---|
> | เป้าหมาย/คน/ปี | 2 · 2 · 2 = 6 เรื่อง | **2 · 2 · 1 = 5 เรื่อง** |
> | ชื่อประเภท | PSIF (Con) · Near miss · PSIF (Behavior) | **PSIF Cardinal Rules · Near Miss · พฤติกรรมตามกิจกรรมเสี่ยงสูง** |
> | Target Plant | เป้า/คน × จำนวนคน | **425 เรื่อง/ปี** (ตัวเลขคงที่ = 5 × 85 คน) |
> | โบนัส | มีส่วน PSIF 5–10% | **ไม่มี — ตัดออกทั้งระบบ** |

## 🌐 เปิดใช้งาน (Live)
**https://topmaha.github.io/PSIF-Clean-Air/**  ← แตะเปิดได้เลย (มือถือ/คอม)

> ✅ **เชื่อมฐานข้อมูลออนไลน์แล้ว** — แอปต่อกับ API `https://psif-cleanair-api.wiphawas-sketchup.workers.dev`
> (Cloudflare D1 + เก็บรูปบน R2) ทุกคนที่เปิดลิงก์จะเห็น/แก้ไขข้อมูลชุดเดียวกันทันที
> ไม่ต้องตั้งค่าอะไร — เปลี่ยน API ได้ที่ ⚙️ ตั้งค่า (ถ้าต้องการ)

## 🚀 เริ่มใช้งานครั้งแรก (ยังไม่มีรายชื่อพนักงานจริง)
ฐานข้อมูลตั้งต้นมี **ผู้ดูแลระบบ 1 คน** ไว้ล็อกอินครั้งแรกเท่านั้น

| ขั้น | ทำอะไร |
|------|---------|
| 1 | เปิดลิงก์ → ล็อกอินด้วยรหัส **`ADMIN`** |
| 2 | เมนู **⚙️ ตั้งค่า** (รหัสผ่านเมนูตั้งค่า: `admin1234` — เปลี่ยนได้ในหน้าตั้งค่า) |
| 3 | **ตั้งค่า → พนักงาน → วางจาก Excel** วางรายชื่อทั้งหมดทีเดียว (ดูหัวข้อ "วางรายชื่อพนักงานจาก Excel") |
| 4 | **ตั้งค่า → พื้นที่/เครื่องจักร** แก้พื้นที่ตัวอย่างให้เป็นพื้นที่จริงของ Clean Air |
| 5 | **ตั้งค่า → เป้าหมาย** ตรวจเป้าปีปัจจุบัน (ค่าตั้งต้น 2 · 2 · 1 = 5 เรื่อง/คน/ปี · Target Plant 425) |
| 6 | เมื่อมีผู้ดูแลตัวจริงแล้ว → **ปิดหรือลบรหัส `ADMIN`** ทิ้ง |

**คอลัมน์ตอนวางรายชื่อ:** `รหัสพนักงาน · ชื่อ-สกุล · แผนก · สิทธิ์` (คั่นด้วย Tab เช่นคัดลอกจาก Excel มาวางตรง ๆ)
ค่าสิทธิ์ที่ใช้ได้: `user` · `teamleader` · `manager` · `dept_admin` · `safety` · `admin`
(เว้นว่าง = `user` · ความหมายของแต่ละสิทธิ์ดูหัวข้อ "สิทธิ์ผู้ใช้")

## ไฟล์ในโปรเจกต์
| ไฟล์ | หน้าที่ |
|------|---------|
| `index.html` | ตัวแอปทั้งหมด (HTML+CSS+JS) เปิดในมือถือ/คอม ได้เลย |
| `worker.js`  | Cloudflare Worker = API เชื่อม D1 + R2 |
| `schema.sql` | โครงสร้างตาราง D1 + ข้อมูลตั้งต้น (ผู้ดูแล 1 คน, ประเภท, พื้นที่, เป้าหมาย 2/2/1 + Target Plant 425) |
| `import-employees.sql` | รายชื่อพนักงานจริง 85 คน + ผู้ส่งที่ลาออกแล้ว 6 คน (จาก sheet **Name**) |
| `import-psif-2026.sql` | ข้อมูล PSIF ปี 2026 จำนวน 564 เรื่อง (จาก sheet **database**) |
| `import-skipped-suggestions.csv` | 29 เรื่องประเภท *Suggestions* ที่ไม่ได้นำเข้า — เก็บไว้ตรวจย้อนหลัง |
| `migrate-2026-09-08-cleanair-targets.sql` | เพิ่มคอลัมน์เป้าหมายรายประเภท (ใช้กับ D1 ที่สร้างก่อน v2.8) |
| `migrate-2026-09-08-import-photo-exempt.sql` | เติม prefix `import-` ให้เรื่องที่นำเข้ารอบแรก เพื่อยกเว้นกฎรูป |

## 🔗 ทรัพยากรบน Cloudflare (ของชุด Clean Air)
| อย่าง | ชื่อ |
|------|------|
| Worker (API) | `psif-cleanair-api` → https://psif-cleanair-api.wiphawas-sketchup.workers.dev |
| D1 database  | `psif-cleanair-db` (`86efa64a-4dfe-4db0-a4c2-33b2c46b14f7`) |
| R2 bucket    | `psif-cleanair-photos` |
| หน้าเว็บแอป  | GitHub Pages ของ repo นี้ — deploy อัตโนมัติทุกครั้งที่ push เข้า `main` |

---

## โหมดทดลอง (ไม่ต้องตั้งเซิร์ฟเวอร์)
เปิด `index.html` จากไฟล์ตรง ๆ ได้ทันที → ข้อมูลเก็บในเครื่องนั้น (localStorage) เท่านั้น ไม่แชร์กับคนอื่น
เหมาะกับลองใช้งานก่อน รหัสตัวอย่าง: `ADMIN` (ผู้ดูแลระบบ)
รหัสปลดล็อกเมนูตั้งค่า = `admin1234` (แก้ได้ในโค้ด ตัวแปร `ADMIN_PASSWORD`)

---

---

## ทำให้เป็นฐานข้อมูลออนไลน์ (ทุกคนเห็นข้อมูลเดียวกัน)

> ✅ **ทำเสร็จแล้วทั้งหมด** — หัวข้อนี้เก็บไว้เป็นบันทึกว่าระบบถูกตั้งขึ้นมาอย่างไร
> (ถ้าต้องสร้างใหม่/ย้ายบัญชี ทำตามขั้น A–E ได้เลย)

### A. ติดตั้ง wrangler (เครื่องที่ deploy)
```bash
npm install -g wrangler
wrangler login
```

### B. สร้าง D1 + R2
```bash
wrangler d1 create psif-cleanair-db
wrangler r2 bucket create psif-cleanair-photos
wrangler d1 execute psif-cleanair-db --remote --file=./schema.sql
```
คัดลอก `database_id` ที่ได้จากคำสั่งแรกไปใส่ `wrangler.toml`

### C. `wrangler.toml` (อยู่ในโฟลเดอร์เดียวกับ `worker.js` — ไฟล์จริงในโปรเจกต์นี้ใส่ค่าให้แล้ว)
```toml
name = "psif-cleanair-api"
main = "worker.js"
compatibility_date = "2024-09-01"

[[d1_databases]]
binding = "DB"
database_name = "psif-cleanair-db"
database_id = "86efa64a-4dfe-4db0-a4c2-33b2c46b14f7"

[[r2_buckets]]
binding = "BUCKET"
bucket_name = "psif-cleanair-photos"

# (ไม่บังคับ) ถ้าเปิด Public access / Custom domain ให้ R2 แล้ว
# ใส่ URL ฐานไว้ตรงนี้ เพื่อให้รูปโหลดตรงจาก R2 (ลดภาระ Worker)
# [vars]
# R2_PUBLIC_BASE = "https://pub-xxxxxxxx.r2.dev"
```

### D. Deploy
```bash
wrangler deploy
```
ได้ URL: `https://psif-cleanair-api.wiphawas-sketchup.workers.dev` (ค่านี้ฝังไว้ในแอปแล้ว)

### E. เชื่อมแอปกับ API
เปิดแอป → เมนู **⚙️ ตั้งค่า → ฐานข้อมูลออนไลน์ (API)** → วาง URL → **บันทึก & เชื่อมต่อ**
ทุกคนที่ใส่ URL เดียวกันจะเห็น/แก้ไขข้อมูลร่วมกัน

> **ทางเลือกผ่าน Dashboard (ไม่ใช้ command line):** Cloudflare → Workers & Pages → Create Worker →
> วางโค้ด `worker.js` → ในแท็บ Settings ผูก Binding ชื่อ `DB` (D1) และ `BUCKET` (R2) →
> รัน `schema.sql` ในหน้า D1 Console → Deploy

---

## 🏷️ ประเภท PSIF — กำหนดตายตัว 3 ประเภทเท่านั้น
| ประเภท |
|--------|
| PSIF |
| Near miss |
| Behavior |

ระบบล็อกไว้ 3 ประเภทนี้ (เพิ่ม/ลบไม่ได้) — ข้อมูลเก่าถูกจัดกลุ่มเข้า 3 ประเภทนี้ให้แล้ว

## 📥 นำเข้าข้อมูลจาก Excel (เฉพาะ Admin)
เมนู **⚙️ ตั้งค่า → นำเข้าข้อมูลจาก Excel** (เห็นเฉพาะ Admin)
คัดลอกหลายแถวจาก Excel แล้ววาง (คั่นด้วย Tab อัตโนมัติ) เรียงคอลัมน์:

| 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|
| วันที่ | รหัสพนักงาน | ประเภทเรื่อง | เรื่อง | ข้อเสนอการแก้ไข |

- วันที่รับทั้ง **พ.ศ.** (15/01/2567) และ **ค.ศ.** (2024-01-15) — แปลงปีให้อัตโนมัติ
- **ระบบระบุหน่วยงานให้เอง** จากรหัสพนักงานในทะเบียน (ถ้าพิมพ์ชื่อมาแทนรหัส ก็พยายามจับคู่ชื่อให้)
- ประเภทเรื่องถูกจัดเข้า 3 ประเภทอัตโนมัติ (PSIF / Near miss / Behavior)
- ทุกแถวเข้าระบบเป็นสถานะ "บันทึกเข้าระบบ" รอ Safety ตรวจสอบตามขั้นตอนปกติ
- ไม่มีรูป (ข้อมูลนำเข้า) · มีปุ่ม **ตรวจสอบ/พรีวิว** ก่อนยืนยันนำเข้า

## 🔔 การแจ้งเตือน
เมื่อมีการดำเนินการใด ๆ กับเรื่องที่พนักงานส่งเข้ามา (Safety อนุมัติ/ไม่อนุมัติ, เริ่มดำเนินการ,
ปิดงาน, แก้ไขเนื้อหา, ลบ) ระบบจะส่งแจ้งเตือนไปหา **ผู้รายงาน** โดยอัตโนมัติ —
ดูได้ที่ไอคอน 🔔 มุมขวาบน (ตัวเลขสีแดง = ยังไม่ได้อ่าน, รีเฟรชเองทุก 60 วินาที)

## ✏️ แก้ไขเนื้อหาโดยผู้อนุมัติ
Safety/Admin (คนที่กดอนุมัติเข้าเกต) เปิดเรื่องแล้วกด **"แก้ไขเนื้อหา"** ได้ —
แก้ประเภท หัวข้อ รายละเอียด ข้อเสนอแนะ พื้นที่ เครื่องจักร ของเรื่องที่พนักงานส่งเข้ามา
และระบบจะแจ้งเตือนผู้รายงานให้ทราบ

## โฮสต์ตัวแอป (index.html)
แค่ไฟล์ static ไฟล์เดียว — repo นี้ deploy ขึ้น **GitHub Pages อัตโนมัติ** ทุกครั้งที่ push เข้า `main`
(ดู `.github/workflows/pages.yml`) → https://topmaha.github.io/PSIF-Clean-Air/
แก้ `index.html` แล้ว `git push` อย่างเดียวพอ · ส่วน `worker.js` ต้อง `wrangler deploy` แยก

---

## 📷 การเก็บรูปภาพ (ประหยัดพื้นที่)
- **ไฟล์รูปจริงเก็บบน Cloudflare R2** — ฐานข้อมูล D1 เก็บแค่ "คีย์" (ข้อความสั้น เช่น
  `psif/12/after-….jpg`) ไม่ได้เก็บไฟล์รูป → D1 เล็กมาก ไม่บวม
- **ย่อรูปอัตโนมัติฝั่งผู้ใช้** ก่อนอัปโหลด (ด้านยาว ≤ 1600px, JPEG q0.82 ≈ 250–420 KB/รูป)
  ปรับความชัด/ขนาดได้ที่ตัวแปร `PHOTO_MAX` และ `PHOTO_QUALITY` ใน `index.html`
  - R2 ฟรี 10 GB ≈ ~12,000 งาน (งานละ 2 รูป) — อยากชัดขึ้นเพิ่มเป็น 1920px ได้ (ไฟล์โตขึ้นเล็กน้อย)
  - D1 เก็บแค่คีย์รูป จึงไม่มีทางทำให้ฐานข้อมูลเต็ม
- **ลบรูปออกจาก R2 อัตโนมัติ** เมื่อ Admin ลบรายการ (`DELETE /psif`)
- เสิร์ฟรูป 2 แบบ:
  1. ผ่าน Worker `GET /photo/:key` (ค่าเริ่มต้น — ใช้ได้เลย, รูปเป็นส่วนตัว)
  2. ตรงจาก R2 public/custom domain — ตั้ง `R2_PUBLIC_BASE` (ดูขั้น C) เพื่อลดภาระ/ค่า request ของ Worker
- โหมดทดลอง (ยังไม่ตั้งค่า API): รูปเก็บแบบ base64 ในเครื่อง (localStorage) ชั่วคราวเท่านั้น —
  พอเชื่อม API/deploy Worker แล้ว รูปจะไปอยู่บน R2 ทันที

---

## การทำงานของ 4 ขั้นตอน (ข้อ 1)
1. **บันทึกเข้าระบบ** — พนักงานกรอกฟอร์ม · แนบรูป "ก่อนแก้ไข" **บังคับเฉพาะประเภท PSIF (Con)**
   (ประเภทอื่นซ่อนช่องรูป ไม่ต้องแนบ — ตรวจซ้ำทั้ง frontend และ backend)
2. **Safety อนุมัติ/ไม่อนุมัติ** — เจ้าหน้าที่ Safety/Admin ตรวจเนื้อหา + ดูเรื่องซ้ำ
   · ทางแยก: **↩️ ส่งกลับให้แก้ไข** (v2.4) — เรื่องเด้งกลับไปหาผู้รายงานและ**หยุดเดินงาน**จนกว่าจะแก้แล้วส่งกลับเข้าระบบ
3. **กำลังดำเนินการ** — เริ่มได้เมื่อ **Admin "เปิดดำเนินการ"** ของหน่วยงานนั้น (ข้อ 6) แล้วใส่เลข No.PSIF
4. **เสร็จ** — ปิดงาน · แนบรูป "หลังแก้ไข" บังคับเฉพาะ PSIF (Con)

ระบบกัน **บันทึกซ้ำ** อัตโนมัติ: ปุ่มบันทึกล็อกทันทีที่กด + ทุกการบันทึกส่ง `requestId` (UUID)
ให้ Worker ตรวจซ้ำ (idempotency) และมี unique index ที่ D1 เป็นชั้นสุดท้าย
(ฐานข้อมูลของ Clean Air สร้างจาก `schema.sql` จึงมีดัชนีกันซ้ำนี้อยู่แล้ว)

## สิทธิ์ผู้ใช้ (เช็คจริงที่ Backend — role อ่านจากตาราง employees ผ่าน header `X-Emp-Id`)
- `user` — บันทึก/ปิดงานของตัวเอง (หน่วยงานระบุอัตโนมัติจากทะเบียนพนักงาน)
- `teamleader` — เท่า user + ดูรายคนของกลุ่มตัวเองบนแดชบอร์ด
- `dept_admin` (**Admin แผนก**) และ `manager` (**Manager**) — **สิทธิ์เท่ากันทุกอย่าง (v2.5)**:
  เห็น/ติดตาม/ดำเนินการ (ออกเลข No., ปิดงาน, แก้เนื้อหา, แนบรูปย้อนหลัง, ส่งเรื่องกลับเข้าระบบ)
  เฉพาะแผนกตัวเอง (ช่องหน่วยงาน/VSM = แผนก, กรองที่ query ฝั่ง Worker) · Export Report ได้ ·
  รับการแจ้งเตือนเรื่องใหม่ของแผนก · **เข้าหน้าตั้งค่าไม่ได้** · **แก้ผลตรวจ Safety ไม่ได้**
- `safety` — อนุมัติ Safety + แก้ไขเนื้อหา + Export Report · **เข้าหน้าตั้งค่าไม่ได้**
- `admin` — **Super Admin**: ทุกแผนก + หน้าตั้งค่าทั้งหมด (ข้อมูลหลัก, เปิดดำเนินการ,
  เป้าหมาย, นำเข้า Excel, ลบรายการ)

## 📊 Report (หน้า "ติดตาม")
ปุ่ม **Report** ส่งออกไฟล์ **Excel (.xlsx)** รายละเอียดครบทุกฟิลด์ ตาม filter ที่เลือกอยู่บนหน้าจอ
สิทธิ์: Admin แผนก / Manager (เฉพาะแผนกตัวเอง) / Safety / Super Admin

**v2.7** เพิ่ม 2 คอลัมน์ถัดจาก `No.PSIF` — **วันที่ได้เลข No.PSIF** และ **ห่างจากวันที่บันทึก (วัน)**
(ดูหัวข้อ "🗓️ วันที่ได้เลข No.PSIF" ด้านล่าง)

## 📊 Export Excel — รายงานรายคน สำหรับหัวหน้า (v1.7)
ตอบคำถาม “ใครเหลือกี่เรื่อง ประเภทอะไรบ้าง” — ไฟล์ .xlsx จัดรูปแบบพร้อมส่งหัวหน้า
(ตรึงหัวตาราง · ตัวกรอง · แถบสลับสี · เขียว = ครบเป้า / แดง = ยังขาด)

**เปิดใช้ได้ 2 ทาง**
- แดชบอร์ด → แตะกลุ่ม VSM/หน่วยงาน (หรือชิป **👥 ทีมของฉัน**) → ในป๊อปอัพรายคน กดปุ่ม **📊 Export Excel — รายงานรายคน**
  → ได้เฉพาะกลุ่มนั้น · สิทธิ์: Team Leader ของกลุ่มนั้น, Admin แผนก/Manager, Safety, Super Admin
- แดชบอร์ด → ชิป **📊 Excel รายคน (ทุกกลุ่ม)** → ได้ทุกกลุ่มในไฟล์เดียว
  · สิทธิ์: Admin แผนก/Manager (เห็นเฉพาะแผนกตัวเอง) / Safety / Super Admin

**3 ชีตในไฟล์เดียว**
1. **รายคน** — การ์ดสรุปด้านบน (ทั้งหมด/ครบเป้า/ยังไม่ครบ/ต้องส่งเพิ่มอีกกี่เรื่อง) + ตารางรายคน:
   ทำได้–ขาด แยก 3 ประเภท, เหลือรวม, ประเภทที่ยังขาด, ส่งทั้งหมด, ผ่านเกณฑ์, ความคืบหน้า %, สถานะ, โบนัสส่วน PSIF + แถวรวม
2. **สรุปตามประเภท** — ภาพรวมต่อประเภท (เป้ารวม/ทำได้/ขาดรวม/คนที่ครบ) และแยกตามหน่วยงาน/VSM ว่ากลุ่มไหนต้องเร่ง
3. **ต้องตามงาน** — เฉพาะคนที่ยังไม่ครบ เรียงขาดมาก → น้อย สำหรับหัวหน้าใช้ตามงาน

ตัวเลขทั้งหมดใช้เกณฑ์เดียวกับแดชบอร์ด (นับเฉพาะ Safety **อนุมัติ** + **จบงาน**) และไม่ขึ้นกับคำค้น/หน้าที่เปิดอยู่บนจอ
ไฟล์สร้างด้วย JS ล้วนในเครื่อง (ไม่พึ่ง CDN/ไลบรารีภายนอก) จึงใช้งานออฟไลน์ได้

## 🗓️ วันที่บนหน้าติดตาม (v1.8)
การ์ดแต่ละเรื่องแสดงวันที่ไว้ข้างเลข No.PSIF เพื่อให้ตามงานง่าย
- **บันทึกเข้าระบบ** → `📅 เข้า วว/ดด/ปปปป` (วันที่บันทึกเรื่องเข้าระบบ)
- **เสร็จ** → `🏁 เสร็จ วว/ดด/ปปปป` (วันที่ปิดงาน)
- **Safety · กำลังดำเนินการ · ไม่เข้าข่าย · ซ้ำ** → ไม่แสดงวันที่

เวลาในฐานข้อมูลเก็บเป็น UTC — แอปแปลงเป็นเวลาไทย (+7) ก่อนแสดง เรื่องที่บันทึกช่วงเช้ามืดจึงไม่เลื่อนไปเป็นวันก่อนหน้า
(ในป๊อปอัพรายละเอียดมีทั้ง "วันที่บันทึกเข้าระบบ" และ "วันที่เสร็จ")

## 👥 พนักงานลาออก / เปลี่ยนรหัสพนักงาน (v1.8)
Target/คน คงที่เสมอ และ **ข้อมูลเก่าต้องไม่หาย** แม้พนักงานจะลาออกหรือเปลี่ยนรหัส
จัดการได้ที่ เมนูตั้งค่า → 👥 พนักงาน → ✏️ (เฉพาะ **Super Admin**)

**1) พนักงานลาออก → ย้ายข้อมูลไป "ช่องว่าง" ของแผนก**
- แต่ละแผนกมี "ช่องว่าง" ได้ **1 คน** รหัส `RESIGNED-<แผนก>` (เช่น `RESIGNED-VSM4`)
  สร้างล่วงหน้าได้ที่การ์ด **📦 ช่องว่างข้อมูลพนักงานลาออก** ในหน้าตั้งค่า (หรือระบบสร้างให้อัตโนมัติตอนย้ายครั้งแรก)
- กด **🚪 พนักงานลาออก** → ข้อมูล PSIF ทั้งหมดของคนนั้นย้ายไปรวมที่ช่องว่างของแผนก
  แล้วรหัสเดิมถูก **ปิดใช้งาน** (เข้าระบบไม่ได้ · ไม่ถูกนับเป็นหัวคน) — ข้อมูลเดิมยังอยู่ครบในระบบ
- ช่องว่าง **ไม่ถูกนับเป็นหัวคน** ในเป้าหมาย/รายงานรายคน (แต่เรื่องของช่องว่างยังนับรวมในภาพรวมแผนก)
  จึงล็อกอินด้วยรหัสช่องว่างไม่ได้

**2) เปลี่ยนรหัสพนักงาน (คนเดิม รหัสใหม่)**
- กด **🔁 เปลี่ยนรหัสพนักงาน** → ใส่รหัสใหม่ (เช่น `S-057` → `T-815`)
- ข้อมูล PSIF เดิมทั้งหมด + ประวัติผู้ปิดงาน/ผู้ตรวจ + การแจ้งเตือน ย้ายไปรหัสใหม่ และ **รหัสเก่าถูกลบออก**
  ข้อมูลใหม่หลังจากนี้อยู่ที่รหัสใหม่ทั้งหมด (ถ้ารหัสใหม่มีอยู่แล้ว ระบบจะรวมข้อมูลเข้าด้วยกัน)
- ถ้าเปลี่ยนรหัสของบัญชีที่กำลังใช้งานอยู่ แอปจะอัปเดตตัวตนให้ทันที ไม่ต้องล็อกอินใหม่

ทั้งสองอย่างทำงานผ่าน `POST /employees/transfer` `{from,to,mode:'rename'|'resign'}` — Worker ตรวจสิทธิ์ Super Admin เอง
(ต้อง `wrangler deploy` ก่อนใช้งานจริง)

## 📥 วางรายชื่อพนักงานจาก Excel — ทีเดียวหลายคน (v2.2)
เดิมเพิ่ม/แก้พนักงานได้ทีละคนเท่านั้น v2.2 เพิ่มการ์ด **📥 วางรายชื่อพนักงานจาก Excel** ไว้ใต้ตาราง 👥 พนักงาน
ในหน้าตั้งค่า (เฉพาะ **Super Admin**) — คัดลอกจาก Excel มาวางได้เลย **การ์ดเดียวทำได้ 3 งาน** เลือกโหมดด้านบน

| โหมด | คอลัมน์ที่วาง (คั่นด้วย Tab) | ผลที่เกิด |
|---|---|---|
| ➕ **เพิ่ม/แก้ไขรายชื่อ** | รหัสพนักงาน · ชื่อ-สกุล · หน่วยงาน/VSM · สิทธิ์ | รหัสใหม่ = เพิ่ม · รหัสที่มีอยู่ = แก้ไขข้อมูลเดิม (ไม่สร้างซ้ำ) |
| 🚪 **พนักงานลาออก** | รหัสพนักงาน (คอลัมน์ 2 ใส่ชื่อไว้ตรวจทานได้) | ย้ายข้อมูล PSIF ไป "ช่องว่าง" ของแผนก แล้วปิดรหัสเดิม |
| 🔁 **เปลี่ยนรหัสพนักงาน** | รหัสเดิม · รหัสใหม่ · ชื่อ-สกุล · หน่วยงาน/VSM | ย้ายข้อมูลเดิมไปรหัสใหม่ แล้วลบรหัสเก่าออก |

**ต้องกด 🔎 ตรวจสอบ/พรีวิว ก่อนเสมอ** ปุ่มยืนยันจะเปิดก็ต่อเมื่อพรีวิวแล้ว — พรีวิวบอกครบว่า

- **กี่คน** แยกเป็น เพิ่มใหม่ / อัปเดตของเดิม / ซ้ำ-เหมือนเดิม / ซ้ำในรายการที่วาง / ข้าม
- **ซ้ำกันหรือไม่** — ซ้ำกับรหัสที่มีในระบบ (บอกด้วยว่าจะเปลี่ยนอะไรบ้าง เช่น `หน่วยงาน VSM4 → VSM7`) และซ้ำกันเองในรายการที่วาง (ใช้แถวแรก)
- **หรือเป็นคนเดิมที่เปลี่ยนรหัส** — ถ้าชื่อซ้ำกับพนักงานที่มีอยู่แล้วแต่คนละรหัส จะเตือนว่าเพิ่มแบบนี้จะกลายเป็น 2 คน
  พร้อมปุ่ม **🔁 ย้ายคนเหล่านี้ไปโหมดเปลี่ยนรหัสพนักงาน** (เตรียมรหัสเดิม → รหัสใหม่ให้เสร็จ กดพรีวิวต่อได้เลย)
- **จะย้ายข้อมูลกี่เรื่อง** (โหมดลาออก/เปลี่ยนรหัส) และจะสร้าง "ช่องว่าง" ให้แผนกไหนบ้าง
- ตารางรายแถวทุกแถว (แสดงสูงสุด 200 แถว) พร้อมเหตุผลของแถวที่ถูกข้าม

กันพลาดไว้ให้แล้ว: เว้นคอลัมน์หน่วยงาน/สิทธิ์ของคนที่มีอยู่แล้ว = **ใช้ค่าเดิม ไม่ล้างทิ้ง** · วางสลับ "ชื่อ | รหัส"
ระบบสลับให้เองแล้วบอกไว้ในพรีวิว · ห้ามใช้รหัส `RESIGNED-*` · ลาออก/เปลี่ยนรหัสของบัญชีที่กำลังใช้งานอยู่ถูกกันไว้
(เปลี่ยนรหัสตัวเองผ่านโหมดนี้ได้ แอปสลับตัวตนให้ทันที) · การไล่รหัสไขว้กันเอง (A→B, B→C) ถูกกันไว้ ให้ทำทีละคนแทน

ทำงานผ่าน `POST /employees/bulk` `{mode:'upsert'|'resign'|'rename', rows:[...]}` — Worker ทำทีละแถว
แถวที่พังไม่ทำให้ทั้งชุดล้ม แล้วคืนผลรายแถวมาบอกว่าแถวไหนไม่สำเร็จเพราะอะไร (Super Admin เท่านั้น · ต้อง `wrangler deploy` ก่อนใช้)

## ⏳ เรื่องที่ส่งแล้วแต่ยังไม่นับ — หน้า "ของฉัน" (v1.9)
แก้ปัญหาพนักงานสับสนว่า *"ลงเรื่องไปแล้ว ทำไมตัวเลขไม่ขึ้น"* เพราะระบบนับเฉพาะเรื่องที่ **อนุมัติ + จบงาน**
หน้าแดชบอร์ด → 🙋 **ของฉัน** จึงแยกเรื่องที่ยังไม่นับออกเป็น **3 กลุ่ม** (ทุกเรื่องอยู่กลุ่มเดียวเท่านั้น)

| กลุ่ม | เงื่อนไข | นับผลงาน |
|---|---|---|
| ⏳ **Safety กำลังพิจารณา** | ส่งแล้ว Safety ยังไม่ตัดสิน | ยังไม่นับ |
| 🔧 **กำลังดำเนินการ** | Safety **อนุมัติแล้ว** แต่ยังไม่ปิดงาน | ยังไม่นับ (จะนับเมื่อปิดงาน) |
| ✖️ **ไม่ผ่านการพิจารณา** | ซ้ำ / ไม่เข้าข่าย Cardinal rules / ไม่อนุมัติ | ไม่นับ |

สิ่งที่แสดงเพิ่ม
- **ชิปสรุป 3 กลุ่ม** ใต้กล่องสถิติ — แตะเพื่อดูรายการในกลุ่มนั้น (แตะรายการต่อเพื่อเปิดรายละเอียด)
- **แถบรายประเภท** เพิ่มส่วนแถบลาย: 🟪 กำลังดำเนินการ · 🟧 รอ Safety ต่อจากแถบทึบ (ที่นับแล้ว)
  พร้อมป้าย `🎯 ถ้าผ่านครบ → n/เป้าประเภทนั้น` บอกว่าถ้าอนุมัติหมดจะได้กี่เรื่องต่อประเภท
- **ถ้าเรื่องที่ค้างผ่านครบ → ...** ใต้กล่อง "นับผลงาน" (กี่เรื่อง) และกล่อง "ยังขาด" (จะเหลือขาดกี่เรื่อง)
- ป๊อปอัพ **🎯 เป้าหมายของคุณ** (แตะกล่อง "ยังขาด") มีกล่อง ⏳ สรุปเรื่องค้าง + ถ้าผ่านครบจะเหลือขาดเท่าไร

ตัวเลข **นับผลงาน / เป้าหมาย ยังใช้เกณฑ์เดิมทุกอย่าง** — ส่วนนี้เป็นการแสดงผลเพื่อความเข้าใจเท่านั้น

## 📥 ยอด "ส่งแล้ว" แยกตัวหลัก/ตัวรอง — แดชบอร์ดทั้งโรงงาน (v2.0)
เดิมไทล์ "ส่งแล้ว" แสดงจำนวนเรื่องที่เข้ามา**ทั้งหมด** ทำให้ยอดถูกดันด้วยเรื่องที่ยังไม่ผ่านเกณฑ์
v2.0 แยกเป็น 2 ชั้นในไทล์เดียว

| ชั้น | ประกอบด้วย | แสดงผล | นับเข้า % ของเป้าหมาย |
|---|---|---|---|
| **ตัวหลัก** | Safety อนุมัติแล้ว → 🔧 กำลังดำเนินการ + 🏁 จบงาน | ตัวเลขใหญ่ | ✅ นับ |
| **ตัวรอง** | ⏳ รอ Safety + ✖️ ไม่ผ่าน (ซ้ำ/ไม่เข้าข่าย/ไม่อนุมัติ) | ตัวเล็กจางๆ `+n` และบรรทัดย่อย | ❌ ไม่นับ |

- `% ของเป้าหมาย` ในไทล์คิดจาก **ตัวหลัก ÷ Target** (เดิมคิดจากยอดรวมทั้งหมด)
- ไทล์ "นับผลงาน" เปลี่ยนตัวหารเป็น `% ของที่ผ่าน Safety` (เดิมเทียบกับยอดส่งทั้งหมด)
- **แตะที่ไทล์** เพื่อเปิดคำอธิบายแยกรายกลุ่ม + "ถ้า Safety อนุมัติเรื่องที่รออยู่ทั้งหมด ตัวหลักจะเป็นเท่าไร"
- แถบความคืบหน้า · กราฟรายประเภท · VSM/หน่วยงาน **ใช้เกณฑ์เดิม (อนุมัติ+จบงาน)** ไม่เปลี่ยน
- การ์ด "📊 สถานะงาน" (โดนัท) และกราฟส่งรายเดือน ยังนับ**ทุกเรื่องที่ส่งเข้ามา** ตามเดิม

## 📷 รูป "ก่อน / หลังแก้ไข" ของ PSIF (Con) ต้องครบ + ⚠️ ชิปตามเก็บรูป (v2.1)
กฎเดิม: PSIF (Con) ต้องแนบ**รูปก่อนแก้ไข**ตอนบันทึก และ**รูปหลังแก้ไข**ตอนปิดงาน (ประเภทอื่นไม่บังคับ)
แต่พบว่ามีบางรายการหลุดเข้าระบบมาโดยไม่มีรูป v2.1 จึงทั้ง **อุดช่องโหว่** และ **ตามเก็บย้อนหลัง**

**เห็นได้ทันทีว่ารายการไหนรูปขาด**
- การ์ดในหน้าติดตาม/Safety ขึ้นป้ายแดง `📷 ขาดรูปก่อนแก้ไข` / `ขาดรูปหลังแก้ไข` + โชว์กรอบรูปว่างไว้เสมอ
- ชิป `⚠️ ขาดรูป (n)` ท้ายแถวสถานะหน้าติดตาม — กรองเฉพาะรายการที่ต้องตามเก็บ (**ไม่มีของค้าง = ชิปหายไปเอง**)
- ในรายละเอียดมีกล่องเตือน + ปุ่ม **“📷 แนบรูป…ย้อนหลัง”** ใต้ช่องที่ขาด
  (สิทธิ์: เจ้าของเรื่อง · Admin แผนก/Manager ของแผนกนั้น · Safety · Super Admin — เช็คซ้ำที่ backend)
- Report `.xlsx` เพิ่มคอลัมน์ **ขาดรูป (ต้องแนบย้อนหลัง)** เอาไปไล่ตามเก็บได้

**ไม่นับข้อมูลที่ย้ายมาจากระบบเดิม** (ตามคำขอ) — ถือเป็น "ข้อมูลเก่า" เมื่อเข้าข้อใดข้อหนึ่ง
| เกณฑ์ | ค่า | ตัวแปรใน `index.html` |
|---|---|---|
| อยู่ในชุดนำเข้าเดิม (เฉพาะระบบที่ย้ายข้อมูลมา) | `id` ≤ `LEGACY_MAX_ID` | `LEGACY_MAX_ID` = **0** (Clean Air ไม่มีข้อมูลเก่า) |
| บันทึกก่อนวันที่เริ่มบังคับแนบรูป | ก่อน `PHOTO_RULE_SINCE` | `PHOTO_RULE_SINCE` = **2000-01-01** (บังคับทุกใบ) |
| นำเข้าผ่านเมนู "นำเข้าจาก Excel" | `request_id` ขึ้นต้น `import-` | (worker ติดป้ายให้อัตโนมัติ) |

เรื่องที่ Safety ตีตกแล้ว (ซ้ำ / ไม่เข้าข่าย) ก็ไม่ต้องตามเก็บรูปเช่นกัน
**ปิดงานไม่ถูกบล็อก** ถ้ารูปก่อนแก้ไขหาย — ย้อนไปถ่ายใหม่ไม่ได้แล้ว ระบบแค่เตือนจนกว่าจะแนบครบ
(เลื่อนเส้นแบ่ง "ข้อมูลเก่า" ได้โดยแก้ 2 ค่าบนสุดของบล็อก `รูป "ก่อน / หลังแก้ไข"` ใน `index.html`)

**อุดช่องโหว่ฝั่ง API (`worker.js`)**
- `POST /psif` ที่ `request_id` ซ้ำ (กดบันทึกซ้ำ/ลองใหม่หลังพลาด) เดิมคืนแถวเดิมเฉย ๆ —
  ถ้ารอบก่อนบันทึกแถวสำเร็จแต่ผูกรูปไม่สำเร็จ แถวนั้นจะไม่มีรูปตลอดไป → ตอนนี้เติมรูปที่ยังขาดให้ก่อนคืน
- `PATCH /psif/:id` การแนบรูปเดิม**ไม่เช็คสิทธิ์เลย** → จำกัดเป็น เจ้าของเรื่อง/Admin แผนกนั้น/Safety/Super Admin
  และแนบรูปอย่างเดียวก็ขยับ `updated_at` ให้
- แก้ประเภทเป็น PSIF (Con) ทีหลังแล้วรูปไม่ครบ → เด้งข้อความเตือนทันทีหลังบันทึก

## 🏷️ กรองตามประเภทงาน — หน้าติดตาม + หน้า Safety (v2.1)
แถวชิปสีเหลืองเหนือชิปสถานะ: `ทุกประเภท · PSIF (Con) · Near miss · PSIF (Behavior)`
- ใช้ร่วมกับตัวกรองเดิมได้ทั้งหมด (ของฉัน/ทั้งหมด/เลือกแผนก · สถานะ · ค้นหา)
- ตัวเลขในวงเล็บของทุกชิป = "ถ้ากดชิปนี้จะเห็นกี่รายการ" (คิดจากตัวกรองอื่นที่เปิดอยู่แล้ว)
- ปุ่ม **Report** ส่งออกตามประเภทที่เลือกอยู่ด้วย (ใช้ตัวกรองชุดเดียวกับที่เห็นบนจอ)

## 🏭 ย้ายแผนกแล้วใบเก่าต้องตามไปด้วย (v2.3)
**อาการที่เจอ:** แดชบอร์ดขึ้นชื่อแผนกที่ **ไม่มีแล้ว** (Program · Lean · Engineer) พร้อมตัวเลข
ส่วนแผนกจริง (**Sales · CI · Engineering**) ขึ้น **0** ทั้งที่กด 👥 รายคน แล้วเห็นข้อมูลอยู่ครบ

**เหตุ:** ตาราง `psif` เก็บ `vsm` (หน่วยงาน) ไว้ในใบ **ตั้งแต่ตอนกดส่ง** พอพนักงานย้ายแผนก
หรือแผนกเปลี่ยนชื่อ ใบเก่าก็ยัง **ค้างอยู่แผนกเดิม** ขณะที่หน้า "รายคน" ยึด `employees.vsm` (แผนกปัจจุบัน) — ตัวเลขสองหน้าจึงไม่ตรงกัน
ผลกระทบไม่ได้มีแค่แดชบอร์ด:

- **Admin แผนก** ของแผนกใหม่ **มองไม่เห็นใบเก่า** — backend scope ด้วย `WHERE vsm=?`
- ประตู **"เปิดดำเนินการ"** (`issuances`) ไปถามหาแผนกเดิมที่ไม่มีแล้ว → กดดำเนินการต่อไม่ได้
- **แจ้งเตือน** ตอนส่งเรื่องใหม่ วิ่งไปหา Admin ของแผนกเดิม
- ช่อง **🏭 เลือกแผนก…** บนหน้าติดตาม ไม่มีชื่อแผนกเดิมให้เลือก → ใบเก่าหาไม่เจอ

**แก้ 3 ชั้น**

| ชั้น | ที่ไหน | ทำอะไร |
|---|---|---|
| กันไม่ให้เกิดใหม่ | `worker.js` → `syncPsifVsm()` | ทุกครั้งที่ **แก้แผนกพนักงาน** (ทีละคน · วางจาก Excel · ลาออก/เปลี่ยนรหัส) ใบ PSIF ของคนนั้นย้ายแผนกตามทันที |
| ล้างของเก่าที่ค้าง | คำสั่ง `UPDATE` ด้านล่าง | ดึง `psif.vsm` ให้ตรงกับ `employees.vsm` ทั้งตาราง — **รันซ้ำได้** |
| กันเหนียวฝั่งจอ | `index.html` → `itemDept()` | การจัดกลุ่ม/กรองบนหน้าจอ ยึด **แผนกปัจจุบันของเจ้าของเรื่อง** ก่อนเสมอ ตกมาที่ `i.vsm` เมื่อไม่พบรหัสในทะเบียน |

```bash
# ดูว่ามีใบค้างแผนกเดิมกี่ใบ (ไม่แก้อะไร) — ปกติของ Clean Air ต้องได้ 0 แถว
wrangler d1 execute psif-cleanair-db --remote --command "SELECT TRIM(COALESCE(p.vsm,'')) old_vsm, TRIM(COALESCE(e.vsm,'')) new_vsm, COUNT(*) n FROM psif p JOIN employees e ON e.id=p.reporter_id COLLATE NOCASE WHERE TRIM(COALESCE(p.vsm,''))<>TRIM(COALESCE(e.vsm,'')) GROUP BY 1,2"

# ถ้าเจอใบค้าง (เช่นแก้แผนกตรงฐานข้อมูลเอง) — ดึงให้ตรงกับทะเบียนพนักงาน รันซ้ำได้
wrangler d1 execute psif-cleanair-db --remote --command "UPDATE psif SET vsm=(SELECT TRIM(COALESCE(e.vsm,'')) FROM employees e WHERE e.id=psif.reporter_id COLLATE NOCASE) WHERE EXISTS (SELECT 1 FROM employees e WHERE e.id=psif.reporter_id COLLATE NOCASE)"
```

> **ใบที่ไม่พบรหัสผู้ส่งในทะเบียนพนักงาน จะไม่ถูกแตะ** (JOIN ไม่ติด) — ใบพวกนี้ตกไปกลุ่ม **(ไม่ระบุ)** บนแดชบอร์ด
> วิธีแก้คือเพิ่มรายชื่อรหัสนั้นในหน้าตั้งค่าก่อน แล้วรันไฟล์ `.sql` ซ้ำอีกครั้ง

## ↩️ Safety เปลี่ยนประเภทแล้วรูปไม่ตรงกฎ — ส่งกลับให้ถ่ายใหม่ / เก็บเป็นรูปประกอบ (v2.4)

**อาการที่เจอ:** พนักงานเลือก "ประเภท" ผิดตั้งแต่ตอนบันทึก Safety จึงแก้ประเภทให้ใหม่ตอนตรวจ
แต่กฎรูปผูกกับประเภท พอเปลี่ยนประเภทแล้วรูปเลยไม่ตรงกฎ เกิด 2 กรณี

| กรณี | พนักงานเลือก | จริง ๆ คือ | ผลหลัง Safety แก้ประเภท | v2.4 จัดการยังไง |
|---|---|---|---|---|
| 1 | Near miss / PSIF (Behavior) — *ไม่ต้องมีรูป* | **PSIF (Con)** | ไม่มีรูปก่อน · มีแต่รูปหลัง | **ส่งกลับให้ผู้รายงานไปถ่ายรูปก่อนแก้ไขมาใหม่** + หยุดเดินงาน |
| 2 | **PSIF (Con)** — *ต้องมีรูป* | Near miss / PSIF (Behavior) | มีรูปก่อน · ไม่มีรูปหลัง | รูปเดิม**ไม่ถูกลบ** เก็บเป็น **"รูปประกอบ"** · ไม่บังคับรูปหลังอีกต่อไป |

### กรณี 1 — สถานะใหม่ `returned` = "↩️ ส่งกลับให้แก้ไข"
- **เกิดอัตโนมัติ** ทันทีที่มีการเปลี่ยนประเภทเป็น PSIF (Con) บนเรื่องที่ยังไม่มีรูปก่อนแก้ไข
  (บังคับที่ **backend** ด้วย — Admin แผนก หรือ client รุ่นเก่าก็เลี่ยงไม่ได้)
- **กดส่งกลับเองก็ได้** — ปุ่ม **"↩️ ส่งกลับให้ผู้รายงานแก้ไข"** ในหน้ารายละเอียด (Safety/Super Admin)
  ใช้ได้ทุกเหตุผล: รูปเบลอ · เนื้อหาไม่ตรงกับที่พบจริง · ข้อมูลไม่ครบ — ต้องกรอกเหตุผลเสมอ
- **บล็อกจริง** — ระหว่างอยู่สถานะนี้ **อนุมัติ / ออกเลข No. / ปิดงานไม่ได้** (แม้แต่ Super Admin)
  และผลตรวจ Safety ถูกรีเซ็ตเป็น *รอตรวจ* เพราะประเภทเปลี่ยนแล้วต้องตรวจใหม่อยู่ดี
- **ทางกลับ** — ผู้รายงานแตะปุ่ม **"📷 ถ่ายรูปก่อนแก้ไขใหม่"** ใต้ช่องรูปที่ขาด
  พอแนบครบ ระบบ**ส่งกลับเข้าคิว Safety ให้อัตโนมัติ** (สถานะกลับเป็น *บันทึกเข้าระบบ*)
  · backend เช็คซ้ำ: ยังไม่มีรูปก่อนแก้ไข = ส่งกลับเข้าระบบไม่ได้
- **แจ้งเตือน 2 ทาง** — ผู้รายงานได้ `↩️ Safety ส่งเรื่อง "…" กลับให้แก้ไข — <เหตุผล>` ·
  พอแก้เสร็จ **คน Safety ที่กดส่งกลับ** ได้ `📨 … ส่งกลับเข้าระบบแล้ว — รอตรวจอีกครั้ง`
  (ไม่งั้นเรื่องจะไปนอนท้ายคิวโดยไม่มีใครรู้ว่ามันกลับมาแล้ว)
- **กดผิดก็ถอยได้** — Safety มีปุ่ม **"↪️ ยกเลิกการส่งกลับ"** ดึงเรื่องกลับเข้าคิวเองโดยไม่ต้องรอรูป
- **เห็นที่ไหนบ้าง** — ชิป `↩️ ส่งกลับให้แก้ไข (n)` บนหน้าติดตามและหน้า Safety (โผล่เฉพาะตอนมีของค้าง) ·
  ป้ายชมพูบนการ์ด · วงกลมสถานะบนแดชบอร์ด · กลุ่ม **"↩️ ส่งกลับให้คุณแก้ไข"** บนหน้า "ของฉัน" ·
  คอลัมน์ **เหตุผลที่ส่งกลับ** ใน Report `.xlsx`

**ส่งกลับได้เฉพาะช่วงก่อนเริ่มงานจริง** (สถานะ *บันทึกเข้าระบบ* / *Safety ตรวจแล้ว*) —
เรื่องที่ **เริ่มดำเนินการ/ปิดงานไปแล้ว** ย้อนไปถ่าย "ก่อนแก้ไข" ไม่ได้อีกแล้ว จึงใช้ระบบ
**⚠️ ตามเก็บรูปย้อนหลัง (v2.1)** ตามเดิม — ไม่บล็อกงานที่ทำไปแล้วให้ค้าง
ข้อมูลเก่า/นำเข้าจาก Excel ไม่ถูกกฎนี้ (เกณฑ์เดียวกับตาราง "ข้อมูลเก่า" ด้านบน)

### กรณี 2 — รูปที่เหลือค้างกลายเป็น "รูปประกอบ"
- **ไม่ลบรูปทิ้ง** — รูปที่พนักงานอุตส่าห์ถ่ายมายังอยู่ครบ แค่เปลี่ยนสถานะเป็นของแถม
- **ไม่ตามหารูปหลังอีก** — ไม่ขึ้นป้าย "ขาดรูป" · ไม่ขึ้นกรอบว่าง "ยังไม่มีรูปหลัง" บนการ์ด ·
  หัวข้อในหน้ารายละเอียดเปลี่ยนเป็น **"📷 รูปประกอบ"** พร้อมกล่องอธิบายว่าประเภทนี้ไม่บังคับ
- **จะเติมให้ครบคู่ก็ได้** — ปุ่ม **"📎 แนบรูปหลังแก้ไข (ไม่บังคับ)"** ในหน้ารายละเอียด
  และช่องแนบรูปแบบไม่บังคับในหน้าปิดงาน — ปิดงานได้ตามปกติแม้ไม่แนบ

### ต้องทำอะไรตอน deploy
> ℹ️ **ฐานข้อมูลของ Clean Air สร้างใหม่จาก `schema.sql` จึงมีคอลัมน์ `psif.return_reason` ครบตั้งแต่ต้น** — ไม่ต้องรัน migrate ใด ๆ
> แก้ `worker.js` เมื่อไรค่อย `wrangler deploy`

## 👨‍💼 Manager = สิทธิ์เท่ากับ Admin แผนก (v2.5)

เดิม `manager` เป็นแค่ "หัวหน้าที่ดูตัวเลขได้" — เห็นรายคนของกลุ่มตัวเอง แต่**กดดำเนินการอะไรไม่ได้เลย**
ทำให้ผู้จัดการแผนกต้องรอ Admin แผนกมากดให้ทุกครั้ง ตั้งแต่ v2.5 ทั้งสอง role ใช้ชุดสิทธิ์เดียวกัน

| ทำอะไรได้ | user | teamleader | **manager / dept_admin** | safety | admin |
|---|:--:|:--:|:--:|:--:|:--:|
| บันทึก/ปิดงานเรื่องของตัวเอง | ✅ | ✅ | ✅ | ✅ | ✅ |
| ดูรายคนของกลุ่มตัวเอง (drill-down) | — | ✅ | ✅ | ✅ (ทุกกลุ่ม) | ✅ (ทุกกลุ่ม) |
| เห็นทุกใบ **ของแผนกตัวเอง** ในหน้าติดตาม | — | — | ✅ | ✅ (ทุกแผนก) | ✅ (ทุกแผนก) |
| ออกเลข No.PSIF · เริ่มดำเนินการ · ปิดงาน · แก้เนื้อหา | — | — | ✅ เฉพาะแผนกตัวเอง | บางส่วน | ✅ |
| แนบรูปย้อนหลัง · ส่งเรื่องกลับเข้าระบบ | เรื่องตัวเอง | เรื่องตัวเอง | ✅ เฉพาะแผนกตัวเอง | ✅ | ✅ |
| Report .xlsx + Excel รายคน | — | เฉพาะกลุ่มตัวเอง | ✅ เฉพาะแผนกตัวเอง | ✅ | ✅ |
| แจ้งเตือนเมื่อมีเรื่องใหม่ในแผนก | — | — | ✅ | ✅ | ✅ |
| บันทึกผลตรวจ Safety (อนุมัติ/ไม่อนุมัติ/ส่งกลับ) | — | — | ❌ | ✅ | ✅ |
| หน้าตั้งค่า (ข้อมูลหลัก/เป้าหมาย/นำเข้า/ลบ) | — | — | ❌ | ❌ | ✅ |

**ทำยังไง** — รวมรายชื่อ role ไว้จุดเดียวทั้งสองฝั่ง แล้วให้ทุกจุดเช็คผ่านตัวนี้ตัวเดียว
ไม่ต้องไล่แก้ทีละ endpoint (เสี่ยงหลุดสิทธิ์บางที่)

| ไฟล์ | ตัวกลาง | จุดที่ใช้ |
|---|---|---|
| `worker.js` | `DEPT_ADMIN_ROLES` / `isDeptAdminRole()` | scope `GET /psif`, `GET /psif/:id`, สิทธิ์ `PATCH` (`deptA`), รายชื่อผู้รับแจ้งเตือน |
| `index.html` | `DEPT_ADMIN_ROLES` / `isDeptAdmin()` | เมนู/แดชบอร์ด/หน้าติดตาม/`canManage()`/`canAddPhoto()`/Export |

> ⚠️ ไม่ต้องรัน migration — เก็บ role เดิมในตาราง `employees` ตามปกติ
> แค่ตั้ง role เป็น **Manager** ที่ ⚙️ ตั้งค่า → พนักงาน แล้วให้ช่อง **หน่วยงาน/VSM** ตรงกับแผนกที่จะให้ดูแล
> (ถ้าเว้นว่าง จะเห็นเฉพาะใบที่ `vsm` ว่างเหมือนกัน — เหมือน Admin แผนกทุกประการ)

---

## ⚡ ทำให้เร็วขึ้นเมื่อข้อมูลเยอะ (v2.6)

พอเรื่องสะสมถึงหลักพัน ระบบเริ่มอืดชัดเจน — เปิดแอปรอนาน กดพิมพ์ค้นหาแล้วหน่วง
ไล่จับต้นเหตุด้วยการจับเวลาจริง เจอ 4 จุด (ไม่ได้แก้ที่ "โค้ดเขียนไม่ดี" แต่แก้ที่ **จำนวนรอบไป-กลับ**)

### 1. ดึงรูปทีละ 90 id ต่อกันเป็นสิบ ๆ รอบ — ต้นเหตุใหญ่สุด
`GET /psif` ดึงรายการมา 1 รอบ แล้ววนดึงรูปครั้งละ 90 id → **2,256 เรื่อง = 26 รอบ D1 ต่อกัน**
วัดได้: TTFB **1.7–2.3 วินาที** (เทียบกับ `/health` ที่ 0.34 วินาที)

แก้เป็น: ยิงรายการ + รูป ไปใน `DB.batch()` **รอบเดียว** โดยให้ SQL เลือก id เองด้วย subquery
(`WHERE psif_id IN (SELECT id FROM psif ...)`) จึงไม่ติดลิมิตพารามิเตอร์ ~100 ตัวของ D1
ส่วน `attachPhotos()` ที่ยังใช้กับรายการเดี่ยว ถ้าเจอชุดใหญ่จะดึงเป็น "ช่วง id" ครั้งเดียวแทน

### 2. เปิดแอปทีเดียว โหลดข้อมูลก้อนใหญ่ 2 รอบ
`bootstrap()` ดึง `/psif` ตอนยังไม่ล็อกอิน แล้ว `enterApp()` ดึงซ้ำอีกรอบหลังรู้ตัวตน
= **2.1 MB × 2** ทุกครั้งที่เปิดแอป ทั้งที่รอบแรกใช้ไม่ได้อยู่ดี (backend ต้องรู้ตัวตนก่อนจึง scope ตามแผนกได้)
ตอนนี้ตอนยังไม่ล็อกอินจะข้ามไปเลย เหลือรอบเดียว

### 3. หน้าจอไล่หาแผนกทีละคน — ทุกเรื่อง ทุกครั้งที่วาด
`empVsm()` ใช้ `state.employees.find()` และถูกเรียกผ่าน `itemDept()` **1 ครั้งต่อ 1 เรื่อง**
→ 2,256 เรื่อง × 432 คน ≈ **หนึ่งล้านรอบต่อการวาดหน้าจอ 1 ครั้ง** และหน้าติดตามวาดทีเดียวนับชิปสิบกว่ารอบ
เปลี่ยนเป็น `Map` สร้างครั้งเดียว (ล้างเมื่อทะเบียนพนักงานเปลี่ยน ผ่าน `empChanged()`)

พร้อมกันนั้น หน้าติดตาม/หน้า Safety เลิกกรองชุดข้อมูลใหม่ทุกชิป — กรอง scope+คำค้นครั้งเดียว
แล้วนับทุกชิปจากชุดนั้นในรอบเดียว · เพิ่มหน่วงการพิมพ์ (debounce 180 ms) ที่ช่องค้นหาทั้ง 3 จุด
· `refreshAll()` วาดเฉพาะหน้าที่เปิดอยู่ (เดิมวาดแดชบอร์ดทุกครั้งแม้ผู้ใช้อยู่หน้าอื่น)

**วัดผลจริง** (ข้อมูล 2,256 เรื่อง / 432 คน · scope ราย VSM · มีคำค้น):
วาดหน้าติดตาม 1 ครั้ง **31.1 ms → 0.3 ms (เร็วขึ้น ~120 เท่า)** — บนมือถือจะต่างกว่านี้อีกหลายเท่า

### 4. เขียนแจ้งเตือนทีละแถว ตอนกดบันทึก
เรื่องใหม่ 1 เรื่องต้องแจ้ง Super Admin + Safety ทุกคน + Manager ของแผนก = สิบกว่า INSERT ต่อกัน
รวมเป็น `DB.batch()` รอบเดียว (`notifyMany()`) · `GET /notifications` รวม 2 คำสั่งเป็นรอบเดียว
· `POST /import` ส่งครั้งละ 50 แถวแทนทีละแถว (นำเข้า 1,690 แถว เดิม = 1,690 รอบ)

### ต้องทำอะไรตอน deploy
> ℹ️ **ฐานข้อมูลของ Clean Air สร้างใหม่จาก `schema.sql` จึงมีดัชนีสำหรับความเร็ว (`created_at`, `(vsm, created_at)`, `(employee_id, id)`) ครบตั้งแต่ต้น** — ไม่ต้องรัน migrate ใด ๆ
> แก้ `worker.js` เมื่อไรค่อย `wrangler deploy`

> 💡 **รูปภาพ** — ตอนนี้รูปวิ่งผ่าน Worker ทุกครั้ง ถ้าอยากให้เร็วกว่านี้อีก ให้เปิด public access
> ของ R2 แล้วตั้งตัวแปร `R2_PUBLIC_BASE` ใน `wrangler.toml` (โค้ดรองรับอยู่แล้ว — ดูหัวข้อ
> "📷 การเก็บรูปภาพ") แอปจะโหลดรูปตรงจาก R2 ไม่ต้องผ่าน Worker เลย

---

## 🗓️ วันที่ได้เลข No.PSIF — ตรวจว่าเรื่องนี้ลงเลขเดือนไหน (v2.7)

**ปัญหา:** พนักงานส่งเรื่องเข้ามาเดือน 7 แต่ Admin ลงเลข No.PSIF ให้เดือน 8
รายงานเดิมมีแต่ *วันที่บันทึก* (`created_at`) เวลาสรุป/ตามงานรายเดือนจึงคลาดเคลื่อน —
มองไม่ออกว่าเรื่องค้างอยู่ที่ขั้น "รอลงเลข" นานแค่ไหน

**สิ่งที่เพิ่ม:** คอลัมน์ `no_at` ในตาราง `psif` = วัน-เวลาที่ **เลขถูกลงครั้งแรก**
- **Worker ประทับเวลาให้เอง** ตอนที่ฟิลด์ `no` ถูกบันทึก (ทั้งปุ่ม *เริ่มดำเนินการ*, ปุ่ม
  *💾 บันทึกเลข* ของ Safety และหน้า *แก้ไขเนื้อหา*) — **ไม่รับค่าจากฝั่งแอป** จึงปลอมวันที่ไม่ได้
- ประทับเฉพาะตอน **เลขเปลี่ยนจริง และยังไม่เคยมีวันที่** — แก้เนื้อหาเฉย ๆ (หน้าแก้ไขส่งเลขเดิม
  กลับมาด้วยทุกครั้ง) หรือแก้เลขที่พิมพ์ผิดของเรื่องที่มีวันที่แล้ว **จะไม่ทำให้วันที่ขยับ**
- **ล้างเลขทิ้ง = ล้างวันที่ตามไปด้วย**

**เห็นได้ที่ไหน**
- **Report (.xlsx)** หน้าติดตาม — 2 คอลัมน์ถัดจาก `No.PSIF`: **วันที่ได้เลข No.PSIF** และ
  **ห่างจากวันที่บันทึก (วัน)** (ตัวเลขนี้แหละที่ทำให้เห็นทันทีว่าเรื่องข้ามเดือนไปลงเลข)
- **ป๊อปอัพรายละเอียด** — บรรทัด *วันที่ได้เลข No.PSIF* เช่น `11/08/2026 (หลังบันทึก 14 วัน)`
- **การ์ดรายการ** — ชี้/แตะค้างที่ป้าย `📄 เลข` จะเห็นวันที่ได้เลข
- **CSV** (ตั้งค่า → ดึงข้อมูลเป็น Excel) — คอลัมน์ `no_at`

**ข้อมูลเก่า:** ไฟล์ migrate เติมย้อนหลังให้เท่าที่มีหลักฐานจริง — ตอนกด *เริ่มดำเนินการ* ระบบเคย
เขียนแจ้งเตือน `🚀 ... เริ่มดำเนินการแล้ว` ไว้ จึงใช้เวลาของแจ้งเตือนใบแรกเป็นวันที่ได้เลข
เรื่องที่ไม่มีร่องรอยนี้ (นำเข้าจาก Excel เก่า / Safety บันทึกเลขเฉย ๆ) **ปล่อยว่างไว้** —
รายงานจะขึ้น `—` และหน้ารายละเอียดขึ้น *"— (ไม่ได้บันทึกไว้)"* ไม่เดาวันที่ให้ผิด

### ต้องทำอะไรตอน deploy
> ℹ️ **ฐานข้อมูลของ Clean Air สร้างใหม่จาก `schema.sql` จึงมีคอลัมน์ `psif.no_at` ครบตั้งแต่ต้น** — ไม่ต้องรัน migrate ใด ๆ
> แก้ `worker.js` เมื่อไรค่อย `wrangler deploy`

---

## ตรรกะการนับ (KPI/แดชบอร์ด/เป้าหมาย)
นับเป็น "ผลงาน" เฉพาะเรื่องที่เข้า **ทั้ง 2 เงื่อนไข**: Safety **อนุมัติ** และดำเนินการ**จนจบ (เสร็จ)**
นอกนั้นไม่นับทุกกรณี (ไทล์ "ส่งแล้ว" และกราฟส่งรายเดือน เป็นเมตริกการส่ง แสดงประกอบเท่านั้น)
เรื่องที่ **↩️ ส่งกลับให้แก้ไข** (v2.4) นับเป็น "ตัวรอง" เหมือนเรื่องที่รอ Safety — ยังไม่เข้าเป้าหมาย
แต่ยังนับใน "ถ้าเรื่องที่ค้างผ่านครบ → กี่เรื่อง" บนหน้า "ของฉัน" เพราะแก้แล้วยังไปต่อได้

---

## 🎯 เป้าหมายของ TENNECO Clean Air (v2.8)

### เป้ารายคน/ปี — แยกรายประเภท
| ประเภท (ชื่อในระบบ) | ค่าที่เก็บใน DB | เป้า/คน/ปี |
|---|---|---|
| **PSIF Cardinal Rules** | `PSIF` | **2** |
| **Near Miss** | `Near miss` | **2** |
| **พฤติกรรมตามกิจกรรมเสี่ยงสูง** | `Behavior` | **1** |
| | | **รวม 5 เรื่อง/คน/ปี** |

> ⚠️ **ส่งเกินประเภทหนึ่งไม่ชดเชยอีกประเภท** — ส่ง Near Miss 5 เรื่องแต่ไม่มี Cardinal Rules เลย ยังถือว่า "ขาด 3"
> ตัวเลข "เหลือ/ขาด" ทุกหน้าจอและทุกรายงานคิดด้วยตรรกะเดียวกัน (ฟังก์ชัน `leftOf()`)

### Target Plant = **425 เรื่อง/ปี**
เป็น**ตัวเลขคงที่**ที่ตกลงกันไว้ (= 5 เรื่อง/คน × 85 คน) ไม่ให้ขยับเองตามจำนวนคนที่เปลี่ยนระหว่างปี
ไทล์ Target บนแดชบอร์ดแสดงทั้งค่าที่ตั้งไว้ และค่าที่คำนวณจากหัวคนปัจจุบันไว้เทียบกัน
(Admin แผนก/Manager ที่เห็นเฉพาะแผนกตัวเอง จะใช้ค่าที่คำนวณจากหัวคนในแผนกแทน)

### 🚫 ไม่มีการคิดโบนัส
ต่างจากระบบ PSIF ของโรงงานเดิม — ชุด Clean Air **ตัดส่วนโบนัสออกทั้งหมด**
ทั้งกล่องบนหน้า "ของฉัน", ป๊อปอัพคำอธิบาย, คอลัมน์ในรายงาน Excel/CSV และฝั่ง API
กล่องส้มบนหน้า "ของฉัน" แสดง **"ยังขาดกี่เรื่อง / ครบเป้าแล้ว"** แทน

### แก้เป้าหมายได้ที่ไหน
⚙️ **ตั้งค่า → 🎯 เป้าหมาย** (เฉพาะ Super Admin) — ตั้งแยกรายประเภทต่อปี + Target Plant
มีปุ่ม **🧮 คำนวณจากจำนวนคน** ช่วยคิด Target Plant = (เป้ารวม/คน) × จำนวนพนักงานที่ยังทำงานอยู่
ค่าทั้งหมดเก็บในตาราง `targets` (`t_psif` · `t_near_miss` · `t_behavior` · `plant_target`)

---

## 📦 นำเข้าข้อมูลจริงจาก Databases PSIF 2026.xlsx

ไฟล์ต้นทาง: `Databases PSIF 2026.xlsx` — 2 sheet

| sheet | ใช้ทำอะไร | คอลัมน์ที่ใช้ |
|---|---|---|
| **Name** | ทะเบียนพนักงาน + แผนก | `ID` · `Name` · `Dept.` · `Position` |
| **database** | เรื่อง PSIF ย้อนหลัง | วันที่ส่ง · หมายเลข PSIF (`Event Title`) · วันที่ปิด · รหัสผู้ส่ง · เรื่อง (`Detail (TH)`) · การแก้ไข (`Action Detail`) · ประเภท (`P-SIF/5S`) |

### สั่งนำเข้า (ทำครั้งเดียว)
```bash
wrangler d1 execute psif-cleanair-db --remote --file=./import-employees.sql
wrangler d1 execute psif-cleanair-db --remote --file=./import-psif-2026.sql
```
รันซ้ำได้ไม่ทำให้ข้อมูลซ้ำ — ทุกเรื่องมี `request_id = xlsx2026-<ลำดับใน Excel>` เป็นกุญแจกันซ้ำ

### สิ่งที่ระบบแปลงให้ตอนนำเข้า
| Excel | → ระบบ |
|---|---|
| `PSIF-CR` และ `PSIF` | ประเภท **PSIF Cardinal Rules** (305 เรื่อง) |
| `Behavior` และ `Behavior not high risk` | ประเภท **พฤติกรรมตามกิจกรรมเสี่ยงสูง** (140 เรื่อง) |
| `Near miss` | ประเภท **Near Miss** (119 เรื่อง) |
| `Suggestions` (29 เรื่อง) | **ไม่นำเข้า** — ระบบมี 3 ประเภทเท่านั้น เก็บไว้ที่ `import-skipped-suggestions.csv` |
| มีวันที่ปิด | สถานะ **เสร็จ** + Safety อนุมัติ (507 เรื่อง) |
| `On Process` | สถานะ **กำลังดำเนินการ** (8 เรื่อง) |
| `Not yet processed` | สถานะ **บันทึกเข้าระบบ** รอ Safety (49 เรื่อง) |
| `Event Title` (26-CHE-xxxxxxx) | เลข **No.PSIF** (306 เรื่องที่มีเลข) |
| `Cardinal Rules` · `Level` · `Responser` · `Due Date` | ต่อท้ายช่อง **รายละเอียด** ไว้ไม่ให้ข้อมูลหาย |

### เรื่องรูปภาพ — ยกเว้นให้ข้อมูลเก่า
ข้อมูลเก่า**ไม่มีรูปก่อน/หลัง** และตามไปถ่ายย้อนหลังไม่ได้ ระบบจึง**ไม่ทวงรูป**กับเรื่องที่นำเข้า
กลไก: แอปยกเว้นกฎรูปให้แถวที่ `request_id` ขึ้นต้นด้วย **`import-`** (ฟังก์ชัน `isLegacyItem`)
ทุกเรื่องที่นำเข้าจึงใช้ `import-xlsx2026-<ลำดับใน Excel>`

> ⚠️ **นำเข้าย้อนหลังรอบใหม่ต้องใช้ prefix `import-` ด้วย** ไม่งั้น PSIF Cardinal Rules ที่นำเข้ามา
> จะขึ้นป้าย 📷 ขาดรูป และไปกองในชิป **⚠️ ขาดรูป** ทั้งที่ไม่มีใครตามเก็บได้
> (ถ้าพลาดไปแล้ว รัน `migrate-2026-09-08-import-photo-exempt.sql` เติม prefix ให้ย้อนหลัง)

**เรื่องใหม่ที่บันทึกผ่านแอปยังบังคับรูปเหมือนเดิมทุกอย่าง** — PSIF Cardinal Rules ต้องมีรูปก่อนตอนบันทึก
และรูปหลังตอนปิดงาน ไม่ครบยังส่งกลับให้แก้ไขตามปกติ

### สิทธิ์ที่ตั้งให้อัตโนมัติ
| กลุ่ม | สิทธิ์ | จำนวน |
|---|---|---|
| `680001` Pannaphat Thongchon (EHS ผู้ดูแลไฟล์ PSIF) | `admin` (Super Admin) | 1 |
| แผนก EHS คนอื่น | `safety` | 1 |
| Position = Manager | `manager` | 8 |
| Position = Supervisor | `teamleader` | 6 |
| ที่เหลือ | `user` | 69 |

> เปลี่ยนได้ทุกเมื่อที่ ⚙️ **ตั้งค่า → พนักงาน**
> รหัส `ADMIN` ชั่วคราวถูก**ปิดใช้งาน**อัตโนมัติเมื่อรัน `import-employees.sql`

### ผู้ส่งที่ไม่มีชื่อในทะเบียน
มี 6 รหัสที่เคยส่ง PSIF แต่ไม่มีใน sheet Name (`650004` · `670002` · `680002` · `SRP-029` · `SRP-104` · `SRP-114`)
ระบบสร้างเป็นพนักงานสถานะ **ลาออก** (`active=0`) — ข้อมูลเก่ายังมีเจ้าของ ดูย้อนหลังได้
แต่**ล็อกอินไม่ได้** และ**ไม่มีเป้าหมาย** (ฟังก์ชัน `isNoTargetPerson`)

คนที่ลาออกแล้วจะ:
- แสดงในหน้า **รายคน** พร้อมป้าย 🚪 *ลาออกแล้ว — ไม่มีเป้า* บอกยอดที่เคยทำไว้ แต่ไม่มีแถบความคืบหน้าและไม่มี "เหลือ"
- **ไม่ถูกนับ**ในสรุป "ทั้งหมด n คน / ครบเป้า / ยังไม่ครบ" ของแผนก
- **ไม่อยู่ในชีต "รายคน"** ของรายงาน Excel (ไปรวมอยู่ในบรรทัดสรุปหัวรายงานแทน)

> เกณฑ์เดียวกันนี้ใช้กับ **ช่องว่างข้อมูลพนักงานลาออก** (`RESIGNED-*`) ที่มีอยู่เดิม
> และคิดเหมือนกันทั้งฝั่งหน้าเว็บและฝั่ง API (`no_target` ใน `/report/person`)
